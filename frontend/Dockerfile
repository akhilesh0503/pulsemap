# Phase 1 placeholder — replaced in Phase 6 with React + Deck.gl build
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
RUN echo '<html><head><title>PulseMap</title></head><body style="background:#0a0e1a;color:#fff;font-family:sans-serif;display:flex;align-items:center;justify-content:center;height:100vh;margin:0"><h1>PulseMap — Frontend building in Phase 6</h1></body></html>' \
    > /usr/share/nginx/html/index.html
EXPOSE 80
